# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# --- uv / virtualenv ---
.venv/
venv/
env/
# NOTE: uv.lock IS committed (reproducible installs) — do NOT ignore it.
.uv-cache/

# --- Build / packaging ---
build/
dist/
*.egg-info/
.eggs/
wheels/

# --- Test / coverage ---
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.tox/
.mypy_cache/
.ruff_cache/
.hypothesis/

# --- Env / secrets ---
.env
.env.*
!.env.example

# --- DB / data (local dev) ---
*.sqlite3
*.db
pgdata/
redisdata/

# --- Alembic ---
# Migration scripts ARE committed; only ignore local artifacts.
*.pyc

# --- ML / RL artifacts (heavy, regenerable) ---
models/checkpoints/
*.onnx
*.pth
runs/
tensorboard/
wandb/

# --- Logs ---
*.log
logs/

# --- Editor / OS ---
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
